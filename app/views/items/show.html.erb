<div class="row align-items-center m-auto">

  <div class="col-lg-6 container text-center align-middle mt-5 py-lg-5">
    <%= image_tag @item.image_url, alt: 'cat picture', class: 'rounded border border-primary'%>
  </div>

  <div class="col-lg-6 container mt-5 py-5">
    <div class="card">
      <div class="card-body">
        <h3 class="card-title"><%= @item.title %></h3>
        <h6 class="card-subtitle mb-2 text-muted">Race : <%= @item.title %></h6>
        <p class="card-text">
          <%= @item.description %><br/>
        </p>
      </div>
    </div>
    <div class="row mt-3 mx-3 justify-content-between">
      <h4>Prix : <%= @item.price %> €</h4> <a href="#" class="btn btn-primary"><i class="fas fa-cart-arrow-down"></i> Ajouter au panier</a>
    </div>
  </div>

</div>
  